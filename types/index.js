export {}

/* typal types/index.xml closure noSuppress */
/**
 * @typedef {_goa.Negotiator} Negotiator `@interface` HTTP Content Negotiation In ES6.
 */
/**
 * @typedef {Object} _goa.Negotiator `@interface` HTTP Content Negotiation In ES6.
 * @prop {function(!Array<string>=): string} charset Negotiated charset.
 * @prop {function(!Array<string>=): !Array<string>} charsets Negotiated charsets.
 * @prop {function(!Array<string>=): string} encoding Negotiated encoding.
 * @prop {function(!Array<string>=): !Array<string>} encodings Negotiated encodings.
 * @prop {function(!Array<string>=): string} language Negotiated language.
 * @prop {function(!Array<string>=): !Array<string>} languages Negotiated languages.
 * @prop {function(!Array<string>=): string} mediaType Negotiated mediaType.
 * @prop {function(!Array<string>=): !Array<string>} mediaTypes Negotiated mediaTypes.
 */
